## 1. 实现双标签图层

- [x] 1.1 修改 `static/index.html` 中 `place-labels` 图层的 `text-field`，使用 `format` 表达式实现 CHGIS 地名双行标签（古名深色粗体 + 今名灰色斜体），LLM 推断地名保持单行
- [x] 1.2 启动服务并在地图上验证双标签显示效果（CHGIS 地名显示双行，LLM 推断地名显示单行，popup 行为不变）

## 2. 收尾

- [x] 2.1 提交代码变更