## 1. CSS 布局重构

- [x] 1.1 新增 `.input-only` / `.view-only` 两组 CSS class，通过 `body.mode-view` 控制显隐
- [x] 1.2 侧栏保持 380px 固定宽度，输入区和查看区各自为 flex 列布局
- [x] 1.3 地图区域始终 `flex: 1`，根据模式切换内部状态
- [x] 1.4 新增「返回编辑」按钮样式（次级按钮，与主按钮区分）
- [x] 1.5 新增地图引导态覆盖层样式（半透明提示文字）

## 2. HTML DOM 结构调整

- [x] 2.1 将所有控件按模式分组：输入区包裹截图上传、文本框、提交按钮；查看区包裹结果面板、时间轴
- [x] 2.2 新增「返回编辑」按钮（查看模式顶部）
- [x] 2.3 新增地图引导态覆盖层 DOM 元素
- [x] 2.4 尺度标签合并到查看模式的结果面板中

## 3. JS 模式切换逻辑

- [x] 3.1 实现 `switchToViewMode()` 函数：设置 `body.mode-view`，显示结果面板，隐藏输入区
- [x] 3.2 实现 `switchToInputMode()` 函数：移除 `body.mode-view`，显示输入区，隐藏结果面板
- [x] 3.3 提交成功（handleSSEEvent result）时自动调用 `switchToViewMode()`
- [x] 3.4 「返回编辑」按钮事件绑定到 `switchToInputMode()`
- [x] 3.5 模式切换时保持文本内容和 `_lastExtractData` 不丢失

## 4. 地图行为适配

- [x] 4.1 输入模式 / 查看模式切换时调整地图状态（引导态 vs 结果态）
- [x] 4.2 引导态：center [112,33] zoom 5，显示提示覆盖层
- [x] 4.3 结果态：fitBounds + scale maxZoom，隐藏提示覆盖层
- [x] 4.4 确保图例和时间轴过滤器在不同模式下正确工作

## 5. 验证

- [x] 5.1 打开页面 → 输入模式，地图中国全景
- [x] 5.2 粘贴文本/截图 → 提交 → 自动切换到查看模式，地图聚焦战役
- [x] 5.3 查看模式点击「返回编辑」→ 输入模式，文本框内容保留
- [x] 5.4 重新提交 → 再次切换到查看模式，旧数据被新数据覆盖
- [x] 5.5 查看模式下编辑地名 → 重新渲染 → 地图更新，保持在查看模式
- [x] 5.6 时间轴在查看模式下正常运作，输入模式下隐藏
- [x] 5.7 现有键盘快捷键（Ctrl+Enter、Esc、左右箭头）在两个模式下均正常工作
